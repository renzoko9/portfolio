---
import type { Dictionary } from '../../i18n/utils';
import SectionEyebrow from '../ui/SectionEyebrow.astro';
import Tag from '../ui/Tag.astro';

interface Props {
  t: Dictionary;
}

const { t } = Astro.props;

const tags = ['UX / UI', 'Producto', 'Ing. de Sistemas', 'UNMSM'];
---

<section id="about" class="section py-20">
  <div class="about-grid">
    <div>
      <SectionEyebrow text={t.about_eyebrow} />
      <h2 class="font-sans fw-bold text-3xl tracking-tight text-primary max-w-xs">{t.about_title}</h2>
    </div>
    <div>
      <p class="font-sans text-md text-secondary leading-relaxed mb-5">{t.about_p1}</p>
      <p class="font-sans text-md text-secondary leading-relaxed mb-6">{t.about_p2}</p>
      <div class="flex gap-2 flex-wrap">
        {tags.map((tag) => <Tag label={tag} />)}
      </div>
    </div>
  </div>
</section>
